name: sentry_triage
description: Self-hosted Sentry triage with AI analysis — rule-based noise filtering, Claude-powered severity/root-cause analysis, GitHub ticketing and draft PRs.
publish_to: none
version: 0.2.0

environment:
  sdk: ^3.5.0

dependencies:
  http: ^1.2.0
  path: ^1.9.0
  sqlite3: ^2.4.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_static: ^1.1.0

dev_dependencies:
  test: ^1.26.3
